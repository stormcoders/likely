import org.scalatest._

class DiscreteIIDModelSpec extends FlatSpec {
  behavior of "A Discrete IID Model"
  
  it should "be initialized by a set of symbols and probabilities" in (pending)
  it should "evaluate an entire sequence" in (pending)
  it should "evaluate a sub-sequence" in (pending)
}